{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "globalDependencies": ["src/*", "package.json"],
  "pipeline": {
    "build": {
      "inputs": ["tsup.config.ts"],
      "outputs": ["dist"]
    },
    "lint": {}
  }
}
