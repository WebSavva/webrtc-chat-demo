{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "inputs": ["src/*", "build/*", "package.json", "tsup.config.ts"],
      "outputs": ["dist"]
    },
    "lint": {}
  }
}
