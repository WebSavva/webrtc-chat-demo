{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "inputs": [
        "src/*",
        "public/*",
        "tsconfig.json",
        "package.json",
        "tsconfig.node.json",
        "vite.config.ts",
        "index.html"
      ],
      "outputs": ["dist"]
    }
  }
}
